name: 🐍 Release

on:
  push:
    - feature/build-exe


jobs:
  release:
    name: 🐍 Release
    runs-on: windows-latest

    steps:
      - name: 📂 Checkout code
        uses: actions/checkout@v2
